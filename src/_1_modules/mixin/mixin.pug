
mixin mixin(spec)
	-
		spec = defaultTo(spec, {
			classes : '',
		});

	.mixin(class=spec.classes)&attributes(attributes)
		if block
			block
		else
			p This is an example paragraph for the mixin module
